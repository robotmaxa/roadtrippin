window.SITE_CODE = window.SITE_CODE || {};
window.SITE_CODE["claude-code-2"] = [
 {
  "name": "main.cpp",
  "lang": "cpp",
  "lines": 199,
  "content": "// Road Trip Optimizer\n// ---------------------------------------------------------------------------\n// Given a start and destination (typed by name), finds the highest-scoring\n// chain of stops between them: national parks and natural wonders always\n// outrank cities, every leg is capped at 450 miles/day, and every stop must\n// move strictly closer to the destination than the one before it (no\n// backtracking, though the route doesn't have to be a straight line).\n//\n// Usage:\n//   ./road_trip                                   interactive prompts\n//   ./road_trip \"Denver, CO\" \"Yellowstone\" [days] [milesPerDay]\n//   ./road_trip --list                             show every known location\n//\n// Trip length: the user picks how many days/stops the trip should be. This\n// matters because rule 1 alone (always end up closer to the destination)\n// still permits a route that zig-zags through every scenic stop in the\n// country as long as each hop is legal -- capping the number of stops is\n// what keeps the result a reasonable \"road trip\" instead of a cross-country\n// scenic-collecting marathon.\n// ---------------------------------------------------------------------------\n\n#include <algorithm>\n#include <cmath>\n#include <iomanip>\n#include <iostream>\n#include <memory>\n#include <string>\n#include <vector>\n\n#include \"roadtrip/Geo.hpp\"\n#include \"roadtrip/Location.hpp\"\n#include \"roadtrip/LocationDatabase.hpp\"\n#include \"roadtrip/RouteOptimizer.hpp\"\n\nusing namespace roadtrip;\n\nnamespace {\n\nconstexpr const char *kDataFile = \"data/locations.csv\";\n\nvoid printAllLocations(const LocationDatabase &db) {\n    std::cout << \"Known locations (\" << db.all().size() << \"):\\n\";\n    for (const auto &loc : db.all()) {\n        std::cout << \"  \" << std::left << std::setw(42) << loc.displayName()\n                  << std::setw(16) << loc.categoryLabel()\n                  << \"rating \" << loc.rating << \"\\n\";\n    }\n}\n\nconst Location *resolveLocation(const LocationDatabase &db, const std::string &query,\n                                 const std::string &roleLabel) {\n    auto matches = db.find(query);\n    if (matches.size() == 1) return matches.front();\n    if (matches.empty()) {\n        std::cerr << \"No location found matching \\\"\" << query << \"\\\" for \" << roleLabel << \".\\n\";\n        std::cerr << \"Run with --list to see every supported location.\\n\";\n        return nullptr;\n    }\n    std::cerr << \"\\\"\" << query << \"\\\" matches multiple locations for \" << roleLabel\n              << \" -- be more specific:\\n\";\n    for (auto *m : matches) std::cerr << \"  - \" << m->displayName() << \"\\n\";\n    return nullptr;\n}\n\nvoid printRoute(const RouteResult &r) {\n    std::cout << \"\\n================ BEST ROAD TRIP ROUTE ================\\n\";\n    std::cout << \"Total stops:        \" << (r.stops.size() - 1) << \" (plus start)\\n\";\n    std::cout << \"Total miles driven: \" << std::fixed << std::setprecision(1) << r.totalMiles << \" mi\\n\";\n    std::cout << \"Total scenic score: \" << std::fixed << std::setprecision(1) << r.totalScore << \"\\n\";\n    std::cout << \"-------------------------------------------------------------------------\\n\";\n    std::cout << std::left << std::setw(7) << \"Stop\"\n              << std::setw(32) << \"Location\"\n              << std::setw(16) << \"Category\"\n              << std::right << std::setw(10) << \"Leg (mi)\"\n              << std::setw(12) << \"Total (mi)\" << \"\\n\";\n    std::cout << \"-------------------------------------------------------------------------\\n\";\n\n    Point prev = r.stops.front().pos;\n    for (size_t i = 0; i < r.stops.size(); ++i) {\n        const auto &s = r.stops[i];\n        std::string stopLabel = s.isStart ? \"Start\" : std::to_string(i);\n        std::cout << std::left << std::setw(7) << stopLabel\n                  << std::setw(32) << s.name\n                  << std::setw(16) << (s.isStart ? std::string(\"-\") : s.category)\n                  << std::right << std::setw(10) << std::fixed << std::setprecision(1)\n                  << (s.isStart ? 0.0 : s.legMiles)\n                  << std::setw(12) << std::fixed << std::setprecision(1) << s.cumulativeMiles\n                  << \"\\n\";\n        prev = s.pos;\n    }\n    std::cout << \"===========================================================================\\n\";\n}\n\n} // namespace\n\nint main(int argc, char *argv[]) {\n    std::vector<std::string> args(argv + 1, argv + argc);\n\n    std::unique_ptr<LocationDatabase> db;\n    try {\n        db = std::make_unique<LocationDatabase>(kDataFile);\n    } catch (const std::exception &e) {\n        std::cerr << \"Error: \" << e.what() << \"\\n\";\n        return 1;\n    }\n\n    if (!args.empty() && (args[0] == \"--list\" || args[0] == \"-l\")) {\n        printAllLocations(*db);\n        return 0;\n    }\n\n    std::string startQuery, destQuery;\n    bool cliMode = args.size() >= 2;\n\n    if (cliMode) {\n        startQuery = args[0];\n        destQuery = args[1];\n    } else {\n        std::cout << \"=== Road Trip Optimizer ===\\n\";\n        std::cout << \"(Run with --list to see every supported location.)\\n\\n\";\n        std::cout << \"Enter start location (e.g. \\\"Chicago, IL\\\"): \";\n        std::getline(std::cin, startQuery);\n        std::cout << \"Enter destination location (e.g. \\\"Yellowstone\\\"): \";\n        std::getline(std::cin, destQuery);\n    }\n\n    const Location *start = resolveLocation(*db, startQuery, \"start\");\n    const Location *destination = resolveLocation(*db, destQuery, \"destination\");\n    if (!start || !destination) return 1;\n    if (start->displayName() == destination->displayName()) {\n        std::cerr << \"Start and destination must be different locations.\\n\";\n        return 1;\n    }\n\n    double maxDailyMiles = MAX_DAILY_MILES_CAP;\n    if (cliMode && args.size() >= 4) {\n        maxDailyMiles = std::stod(args[3]);\n    } else if (!cliMode) {\n        std::cout << \"Max miles per day [default \" << MAX_DAILY_MILES_CAP << \", cap \"\n                  << MAX_DAILY_MILES_CAP << \"]: \";\n        std::string line;\n        std::getline(std::cin, line);\n        if (!line.empty()) maxDailyMiles = std::stod(line);\n    }\n    if (maxDailyMiles <= 0 || maxDailyMiles > MAX_DAILY_MILES_CAP + 1e-6) {\n        std::cerr << \"Max daily miles must be > 0 and <= \" << MAX_DAILY_MILES_CAP\n                  << \". Clamping to \" << MAX_DAILY_MILES_CAP << \".\\n\";\n        maxDailyMiles = MAX_DAILY_MILES_CAP;\n    }\n\n    // The direct distance sets a floor on how many days the trip needs; the\n    // suggested default adds a little slack so the optimizer has room to\n    // route through nearby scenic stops instead of being forced in a\n    // straight line.\n    const double directDistance = haversineMiles(start->pos, destination->pos);\n    const int minDays = std::max(1, static_cast<int>(std::ceil(directDistance / maxDailyMiles - 1e-9)));\n    const int suggestedDays = std::min(30, minDays + std::max(2, minDays / 3));\n\n    int tripDays = suggestedDays;\n    if (cliMode && args.size() >= 3) {\n        tripDays = std::stoi(args[2]);\n    } else if (!cliMode) {\n        std::cout << \"Direct distance: \" << std::fixed << std::setprecision(0) << directDistance\n                  << \" mi (minimum \" << minDays << \" day(s) at this daily cap)\\n\";\n        std::cout << \"Trip length in days [default \" << suggestedDays << \"]: \";\n        std::string line;\n        std::getline(std::cin, line);\n        if (!line.empty()) tripDays = std::stoi(line);\n    }\n    if (tripDays < 1 || tripDays > 90) {\n        std::cerr << \"Trip length must be between 1 and 90 days. Clamping.\\n\";\n        tripDays = std::clamp(tripDays, 1, 90);\n    }\n\n    std::vector<Location> candidates;\n    candidates.reserve(db->all().size());\n    for (const auto &loc : db->all()) {\n        if (&loc == start || &loc == destination) continue;\n        candidates.push_back(loc);\n    }\n\n    RouteOptimizer optimizer(maxDailyMiles, tripDays);\n    RouteResult result = optimizer.solve(*start, *destination, candidates);\n\n    std::cout << \"\\nFrom: \" << start->displayName() << \"\\nTo:   \" << destination->displayName() << \"\\n\";\n    std::cout << \"Trip length: \" << tripDays << \" day(s) max, \" << maxDailyMiles << \" mi/day cap\\n\";\n\n    if (!result.found) {\n        std::cout << \"\\nNo valid route found: the destination could not be reached within \"\n                  << tripDays << \" day(s) at \" << maxDailyMiles << \" mi/day (needs at least \"\n                  << minDays << \" day(s) for the direct distance alone). Try more days or a \"\n                  << \"higher daily cap.\\n\";\n        return 1;\n    }\n\n    printRoute(result);\n    return 0;\n}\n"
 },
 {
  "name": "RouteOptimizer.hpp",
  "lang": "cpp",
  "lines": 182,
  "content": "#pragma once\n\n#include <algorithm>\n#include <limits>\n#include <vector>\n\n#include \"Location.hpp\"\n\nnamespace roadtrip {\n\nstruct RouteStop {\n    std::string name;\n    std::string category;\n    Point pos;\n    double legMiles = 0.0;\n    double cumulativeMiles = 0.0;\n    bool isStart = false;\n};\n\nstruct RouteResult {\n    bool found = false;\n    std::vector<RouteStop> stops; // stops[0] is the start, stops.back() is the destination\n    double totalScore = 0.0;\n    double totalMiles = 0.0;\n};\n\n// Finds the highest-scoring chain of at most `maxStops` stops from `start`\n// to `destination`.\n//\n// Rules enforced:\n//   1. One-directional travel: every stop must be strictly closer (great-\n//      circle distance) to the destination than the stop before it. This\n//      allows any detour shape (not just a straight line) while forbidding\n//      backtracking, in any compass direction.\n//   2. No single leg may exceed the max-daily-miles cap (450 by default).\n//   3. The whole trip fits in `maxStops` days/stops -- this is the user's\n//      chosen trip length. Without a cap here, rule 1 alone lets the\n//      optimizer zig-zag through every scenic stop in the country as long\n//      as each individual hop nudges closer to the destination, producing\n//      a technically-legal but absurdly long \"road trip\".\n//   4. National parks / natural wonders always outrank cities (see\n//      Location::score()), so the optimizer prefers routing through scenic\n//      stops whenever the above constraints allow it.\n//\n// Implementation: candidates form a DAG where an edge u -> v exists iff v is\n// closer to the destination than u and the leg fits the daily cap; combined\n// with the day budget this is a classic bounded-hop-count longest path.\n// dp[k][i] = best cumulative score reaching node i as the k-th stop.\n// O(maxStops * n^2), trivial for a few hundred locations.\nclass RouteOptimizer {\npublic:\n    RouteOptimizer(double maxDailyMiles, int maxStops)\n        : maxDailyMiles_(maxDailyMiles), maxStops_(maxStops) {}\n\n    // `candidates` is the pool of possible waypoints; callers should exclude\n    // `start` and `destination` from it beforehand.\n    RouteResult solve(const Location &start, const Location &destination,\n                       const std::vector<Location> &candidates) const {\n        RouteResult result;\n        const double destRemainingAtStart = haversineMiles(start.pos, destination.pos);\n\n        std::vector<const Location *> nodes;\n        std::vector<double> remaining; // distance from node to destination\n        for (const auto &loc : candidates) {\n            double rem = haversineMiles(loc.pos, destination.pos);\n            if (rem < destRemainingAtStart - kProgressEps) {\n                nodes.push_back(&loc);\n                remaining.push_back(rem);\n            }\n        }\n        // Destination is always the final, mandatory node.\n        nodes.push_back(&destination);\n        remaining.push_back(0.0);\n\n        const size_t n = nodes.size();\n        const int K = std::max(1, maxStops_);\n        const double NEG_INF = -std::numeric_limits<double>::infinity();\n\n        // dp[k][i]: best score/miles/parent reaching node i as the k-th stop.\n        std::vector<std::vector<double>> dpScore(K + 1, std::vector<double>(n, NEG_INF));\n        std::vector<std::vector<double>> dpKey(K + 1, std::vector<double>(n, NEG_INF));\n        std::vector<std::vector<double>> dpMiles(K + 1, std::vector<double>(n, 0.0));\n        std::vector<std::vector<int>> parent(K + 1, std::vector<int>(n, -1));\n\n        for (size_t i = 0; i < n; ++i) {\n            double distFromStart = haversineMiles(start.pos, nodes[i]->pos);\n            if (distFromStart <= maxDailyMiles_ + kDistanceEps) {\n                dpScore[1][i] = nodes[i]->score();\n                dpMiles[1][i] = distFromStart;\n                dpKey[1][i] = dpScore[1][i] - kMileageTieBreak * dpMiles[1][i];\n            }\n        }\n\n        for (int k = 2; k <= K; ++k) {\n            for (size_t i = 0; i < n; ++i) {\n                for (size_t j = 0; j < n; ++j) {\n                    if (dpScore[k - 1][j] == NEG_INF) continue;\n                    if (!(remaining[i] < remaining[j] - kProgressEps)) continue;\n                    double leg = haversineMiles(nodes[j]->pos, nodes[i]->pos);\n                    if (leg > maxDailyMiles_ + kDistanceEps) continue;\n\n                    double candidateScore = dpScore[k - 1][j] + nodes[i]->score();\n                    double candidateMiles = dpMiles[k - 1][j] + leg;\n                    double candidateKey = candidateScore - kMileageTieBreak * candidateMiles;\n                    if (candidateKey > dpKey[k][i]) {\n                        dpScore[k][i] = candidateScore;\n                        dpMiles[k][i] = candidateMiles;\n                        dpKey[k][i] = candidateKey;\n                        parent[k][i] = static_cast<int>(j);\n                    }\n                }\n            }\n        }\n\n        const size_t destIdx = n - 1;\n        int bestK = -1;\n        double bestKey = NEG_INF;\n        for (int k = 1; k <= K; ++k) {\n            if (dpScore[k][destIdx] == NEG_INF) continue;\n            if (dpKey[k][destIdx] > bestKey) {\n                bestKey = dpKey[k][destIdx];\n                bestK = k;\n            }\n        }\n        if (bestK == -1) {\n            return result; // destination unreachable within maxStops days\n        }\n\n        std::vector<size_t> path;\n        int curI = static_cast<int>(destIdx);\n        int curK = bestK;\n        while (curI != -1) {\n            path.push_back(static_cast<size_t>(curI));\n            int prevI = parent[curK][curI];\n            --curK;\n            curI = prevI;\n        }\n        std::reverse(path.begin(), path.end());\n\n        result.found = true;\n        result.totalScore = dpScore[bestK][destIdx];\n        result.totalMiles = dpMiles[bestK][destIdx];\n\n        RouteStop startStop;\n        startStop.name = start.displayName();\n        startStop.category = start.categoryLabel();\n        startStop.pos = start.pos;\n        startStop.isStart = true;\n        result.stops.push_back(startStop);\n\n        Point prev = start.pos;\n        double cumulative = 0.0;\n        for (size_t idx : path) {\n            const Location &loc = *nodes[idx];\n            double leg = haversineMiles(prev, loc.pos);\n            cumulative += leg;\n            RouteStop stop;\n            stop.name = loc.displayName();\n            stop.category = loc.categoryLabel();\n            stop.pos = loc.pos;\n            stop.legMiles = leg;\n            stop.cumulativeMiles = cumulative;\n            result.stops.push_back(stop);\n            prev = loc.pos;\n        }\n        return result;\n    }\n\nprivate:\n    double maxDailyMiles_;\n    int maxStops_;\n    // Guards against float noise when comparing remaining/leg distances to\n    // the exact daily cap or to zero progress.\n    static constexpr double kProgressEps = 0.5;   // miles\n    static constexpr double kDistanceEps = 1e-6;  // miles\n    // Purely a tie-breaker between equal-scoring paths (prefer fewer total\n    // miles); far too small to ever override a real scenic-score difference.\n    static constexpr double kMileageTieBreak = 1e-5;\n};\n\n} // namespace roadtrip\n"
 },
 {
  "name": "LocationDatabase.hpp",
  "lang": "cpp",
  "lines": 96,
  "content": "#pragma once\n\n#include <algorithm>\n#include <cctype>\n#include <fstream>\n#include <sstream>\n#include <stdexcept>\n#include <vector>\n\n#include \"Location.hpp\"\n\nnamespace roadtrip {\n\n// Loads the curated CSV of national parks, natural wonders, and cities, and\n// resolves free-text user input (\"Denver, CO\", \"Yellowstone\") to entries.\nclass LocationDatabase {\npublic:\n    explicit LocationDatabase(const std::string &csvPath) { load(csvPath); }\n\n    const std::vector<Location> &all() const { return locations_; }\n\n    // Case-insensitive lookup. An exact match on \"Name\" or \"Name, State\" wins\n    // outright; otherwise every entry whose display name contains the query\n    // is returned so the caller can report ambiguity.\n    std::vector<const Location *> find(const std::string &query) const {\n        std::string q = normalize(query);\n        std::vector<const Location *> exact, partial;\n        for (const auto &loc : locations_) {\n            std::string full = normalize(loc.displayName());\n            std::string nameOnly = normalize(loc.name);\n            if (full == q || nameOnly == q) {\n                exact.push_back(&loc);\n            } else if (full.find(q) != std::string::npos) {\n                partial.push_back(&loc);\n            }\n        }\n        return !exact.empty() ? exact : partial;\n    }\n\nprivate:\n    std::vector<Location> locations_;\n\n    static std::string normalize(std::string s) {\n        std::transform(s.begin(), s.end(), s.begin(),\n                        [](unsigned char c) { return static_cast<char>(std::tolower(c)); });\n        return s;\n    }\n\n    static Category parseCategory(const std::string &s) {\n        if (s == \"NATIONAL_PARK\") return Category::NationalPark;\n        if (s == \"NATURAL_WONDER\") return Category::NaturalWonder;\n        return Category::City;\n    }\n\n    void load(const std::string &csvPath) {\n        std::ifstream in(csvPath);\n        if (!in) {\n            throw std::runtime_error(\"could not open location database: \" + csvPath);\n        }\n\n        std::string line;\n        std::getline(in, line); // header row, discarded\n        int lineNo = 1;\n        while (std::getline(in, line)) {\n            ++lineNo;\n            if (line.empty() || line[0] == '#') continue;\n\n            std::stringstream ss(line);\n            std::string name, state, latS, lonS, catS, ratingS;\n            std::getline(ss, name, ',');\n            std::getline(ss, state, ',');\n            std::getline(ss, latS, ',');\n            std::getline(ss, lonS, ',');\n            std::getline(ss, catS, ',');\n            std::getline(ss, ratingS, ',');\n            if (name.empty() || latS.empty() || lonS.empty()) {\n                throw std::runtime_error(\"malformed row \" + std::to_string(lineNo) + \" in \" + csvPath);\n            }\n\n            Location loc;\n            loc.name = name;\n            loc.state = state;\n            loc.pos.lat = std::stod(latS);\n            loc.pos.lon = std::stod(lonS);\n            loc.category = parseCategory(catS);\n            loc.rating = ratingS.empty() ? 0.0 : std::stod(ratingS);\n            locations_.push_back(std::move(loc));\n        }\n        if (locations_.empty()) {\n            throw std::runtime_error(\"location database is empty: \" + csvPath);\n        }\n    }\n};\n\n} // namespace roadtrip\n"
 },
 {
  "name": "Location.hpp",
  "lang": "cpp",
  "lines": 43,
  "content": "#pragma once\n\n#include <string>\n\n#include \"Geo.hpp\"\n\nnamespace roadtrip {\n\nenum class Category { NationalPark, NaturalWonder, City };\n\n// Natural spots always outrank cities: this bonus exceeds the max possible\n// raw rating (0-5), so any natural location's score beats any city's\n// regardless of how their raw ratings compare.\nconstexpr double NATURAL_BONUS = 5.0;\n\nstruct Location {\n    std::string name;\n    std::string state;\n    Point pos;\n    Category category = Category::City;\n    double rating = 0.0; // raw 0-5 quality score\n\n    double score() const {\n        return rating + (category == Category::City ? 0.0 : NATURAL_BONUS);\n    }\n\n    bool isNatural() const { return category != Category::City; }\n\n    std::string displayName() const {\n        return state.empty() ? name : name + \", \" + state;\n    }\n\n    std::string categoryLabel() const {\n        switch (category) {\n            case Category::NationalPark: return \"National Park\";\n            case Category::NaturalWonder: return \"Natural Wonder\";\n            default: return \"City\";\n        }\n    }\n};\n\n} // namespace roadtrip\n"
 },
 {
  "name": "Geo.hpp",
  "lang": "cpp",
  "lines": 44,
  "content": "#pragma once\n\n#include <cmath>\n#include <string>\n\nnamespace roadtrip {\n\nstruct Point {\n    double lat = 0.0;\n    double lon = 0.0;\n};\n\nconstexpr double EARTH_RADIUS_MILES = 3958.8;\nconstexpr double MAX_DAILY_MILES_CAP = 450.0;\n\n// Great-circle distance in miles, used as the driving-distance estimate\n// since no live routing API is available offline.\ninline double haversineMiles(const Point &a, const Point &b) {\n    constexpr double kPi = 3.14159265358979323846;\n    double lat1 = a.lat * kPi / 180.0, lon1 = a.lon * kPi / 180.0;\n    double lat2 = b.lat * kPi / 180.0, lon2 = b.lon * kPi / 180.0;\n    double dlat = lat2 - lat1, dlon = lon2 - lon1;\n    double h = std::sin(dlat / 2) * std::sin(dlat / 2) +\n               std::cos(lat1) * std::cos(lat2) * std::sin(dlon / 2) * std::sin(dlon / 2);\n    double c = 2 * std::atan2(std::sqrt(h), std::sqrt(1.0 - h));\n    return EARTH_RADIUS_MILES * c;\n}\n\n// 8-point compass heading from a to b, for display only.\ninline std::string compassHeading(const Point &a, const Point &b) {\n    constexpr double kPi = 3.14159265358979323846;\n    double lat1 = a.lat * kPi / 180.0, lat2 = b.lat * kPi / 180.0;\n    double dlon = (b.lon - a.lon) * kPi / 180.0;\n    double y = std::sin(dlon) * std::cos(lat2);\n    double x = std::cos(lat1) * std::sin(lat2) - std::sin(lat1) * std::cos(lat2) * std::cos(dlon);\n    double bearing = std::atan2(y, x) * 180.0 / kPi;\n    bearing = std::fmod(bearing + 360.0, 360.0);\n    static const char *labels[8] = {\"N\", \"NE\", \"E\", \"SE\", \"S\", \"SW\", \"W\", \"NW\"};\n    int idx = static_cast<int>(std::lround(bearing / 45.0)) % 8;\n    return labels[idx];\n}\n\n} // namespace roadtrip\n"
 },
 {
  "name": "locations.csv",
  "lang": "text",
  "lines": 165,
  "content": "name,state,lat,lon,category,rating\nYellowstone National Park,WY,44.60,-110.50,NATIONAL_PARK,5.0\nGrand Teton National Park,WY,43.79,-110.68,NATIONAL_PARK,4.8\nGlacier National Park,MT,48.70,-113.80,NATIONAL_PARK,4.9\nRocky Mountain National Park,CO,40.34,-105.68,NATIONAL_PARK,4.7\nZion National Park,UT,37.30,-113.05,NATIONAL_PARK,4.9\nBryce Canyon National Park,UT,37.59,-112.19,NATIONAL_PARK,4.8\nCapitol Reef National Park,UT,38.37,-111.26,NATIONAL_PARK,4.5\nArches National Park,UT,38.73,-109.59,NATIONAL_PARK,4.8\nCanyonlands National Park,UT,38.20,-109.93,NATIONAL_PARK,4.6\nGrand Canyon National Park,AZ,36.06,-112.14,NATIONAL_PARK,5.0\nPetrified Forest National Park,AZ,34.91,-109.81,NATIONAL_PARK,4.0\nSaguaro National Park,AZ,32.25,-110.50,NATIONAL_PARK,4.2\nMesa Verde National Park,CO,37.18,-108.49,NATIONAL_PARK,4.5\nGreat Sand Dunes National Park,CO,37.73,-105.51,NATIONAL_PARK,4.4\nBlack Canyon of the Gunnison National Park,CO,38.58,-107.72,NATIONAL_PARK,4.3\nCarlsbad Caverns National Park,NM,32.17,-104.44,NATIONAL_PARK,4.4\nWhite Sands National Park,NM,32.78,-106.17,NATIONAL_PARK,4.5\nGuadalupe Mountains National Park,TX,31.92,-104.87,NATIONAL_PARK,4.1\nBig Bend National Park,TX,29.25,-103.25,NATIONAL_PARK,4.5\nDeath Valley National Park,CA,36.50,-117.08,NATIONAL_PARK,4.6\nJoshua Tree National Park,CA,33.87,-115.90,NATIONAL_PARK,4.6\nSequoia National Park,CA,36.49,-118.56,NATIONAL_PARK,4.7\nKings Canyon National Park,CA,36.80,-118.55,NATIONAL_PARK,4.5\nYosemite National Park,CA,37.87,-119.54,NATIONAL_PARK,5.0\nRedwood National Park,CA,41.21,-124.00,NATIONAL_PARK,4.6\nLassen Volcanic National Park,CA,40.49,-121.51,NATIONAL_PARK,4.2\nCrater Lake National Park,OR,42.94,-122.14,NATIONAL_PARK,4.8\nMount Rainier National Park,WA,46.85,-121.76,NATIONAL_PARK,4.7\nOlympic National Park,WA,47.80,-123.60,NATIONAL_PARK,4.7\nNorth Cascades National Park,WA,48.70,-121.20,NATIONAL_PARK,4.4\nBadlands National Park,SD,43.75,-102.50,NATIONAL_PARK,4.4\nWind Cave National Park,SD,43.57,-103.48,NATIONAL_PARK,4.0\nTheodore Roosevelt National Park,ND,46.97,-103.45,NATIONAL_PARK,4.1\nVoyageurs National Park,MN,48.50,-92.88,NATIONAL_PARK,4.2\nIsle Royale National Park,MI,48.10,-88.55,NATIONAL_PARK,4.1\nMammoth Cave National Park,KY,37.19,-86.10,NATIONAL_PARK,4.3\nGreat Smoky Mountains National Park,TN,35.68,-83.53,NATIONAL_PARK,4.8\nShenandoah National Park,VA,38.53,-78.35,NATIONAL_PARK,4.4\nNew River Gorge National Park,WV,37.90,-81.07,NATIONAL_PARK,4.2\nCongaree National Park,SC,33.78,-80.78,NATIONAL_PARK,3.9\nEverglades National Park,FL,25.29,-80.90,NATIONAL_PARK,4.3\nBiscayne National Park,FL,25.49,-80.21,NATIONAL_PARK,3.9\nHot Springs National Park,AR,34.51,-93.06,NATIONAL_PARK,3.8\nAcadia National Park,ME,44.35,-68.21,NATIONAL_PARK,4.6\nCuyahoga Valley National Park,OH,41.24,-81.55,NATIONAL_PARK,4.0\nIndiana Dunes National Park,IN,41.65,-87.05,NATIONAL_PARK,3.9\nMount Rushmore,SD,43.88,-103.46,NATURAL_WONDER,4.0\nNiagara Falls,NY,43.08,-79.07,NATURAL_WONDER,4.7\nAntelope Canyon,AZ,36.86,-111.37,NATURAL_WONDER,4.7\nHorseshoe Bend,AZ,36.88,-111.51,NATURAL_WONDER,4.5\nMonument Valley,UT,36.98,-110.10,NATURAL_WONDER,4.7\nDevils Tower,WY,44.59,-104.71,NATURAL_WONDER,4.4\nMultnomah Falls,OR,45.58,-122.12,NATURAL_WONDER,4.4\nColumbia River Gorge,OR,45.70,-121.90,NATURAL_WONDER,4.3\nLake Tahoe,CA,39.09,-120.03,NATURAL_WONDER,4.6\nHavasu Falls,AZ,36.26,-112.70,NATURAL_WONDER,4.6\nMeteor Crater,AZ,35.03,-111.02,NATURAL_WONDER,3.8\nPainted Desert,AZ,35.70,-109.78,NATURAL_WONDER,4.0\nGoblin Valley State Park,UT,38.56,-110.71,NATURAL_WONDER,4.3\nBoston,MA,42.36,-71.06,CITY,4.0\nProvidence,RI,41.82,-71.41,CITY,3.3\nHartford,CT,41.76,-72.69,CITY,3.0\nNew York,NY,40.71,-74.01,CITY,4.3\nPhiladelphia,PA,39.95,-75.16,CITY,3.8\nBaltimore,MD,39.29,-76.61,CITY,3.4\nWashington,DC,38.91,-77.04,CITY,4.2\nRichmond,VA,37.54,-77.44,CITY,3.3\nRoanoke,VA,37.27,-79.94,CITY,3.1\nRaleigh,NC,35.78,-78.64,CITY,3.4\nCharlotte,NC,35.23,-80.84,CITY,3.4\nAsheville,NC,35.60,-82.55,CITY,3.9\nColumbia,SC,34.00,-81.03,CITY,3.0\nCharleston,SC,32.78,-79.93,CITY,4.0\nSavannah,GA,32.08,-81.09,CITY,3.9\nAtlanta,GA,33.75,-84.39,CITY,3.7\nChattanooga,TN,35.05,-85.31,CITY,3.5\nKnoxville,TN,35.96,-83.92,CITY,3.2\nNashville,TN,36.16,-86.78,CITY,3.9\nMemphis,TN,35.15,-90.05,CITY,3.7\nLouisville,KY,38.25,-85.76,CITY,3.3\nCincinnati,OH,39.10,-84.51,CITY,3.2\nColumbus,OH,39.96,-83.00,CITY,3.0\nCleveland,OH,41.50,-81.69,CITY,3.1\nPittsburgh,PA,40.44,-79.99,CITY,3.4\nBuffalo,NY,42.89,-78.88,CITY,3.0\nAlbany,NY,42.65,-73.75,CITY,2.8\nPortland,ME,43.66,-70.26,CITY,3.6\nDetroit,MI,42.33,-83.05,CITY,3.1\nChicago,IL,41.88,-87.63,CITY,4.2\nIndianapolis,IN,39.77,-86.16,CITY,3.0\nMilwaukee,WI,43.04,-87.91,CITY,3.2\nMinneapolis,MN,44.98,-93.27,CITY,3.7\nFargo,ND,46.88,-96.79,CITY,2.6\nBismarck,ND,46.81,-100.78,CITY,2.5\nBillings,MT,45.78,-108.50,CITY,2.8\nBozeman,MT,45.68,-111.04,CITY,3.4\nMissoula,MT,46.87,-113.99,CITY,3.2\nGreat Falls,MT,47.50,-111.30,CITY,2.6\nSpokane,WA,47.66,-117.43,CITY,3.0\nSeattle,WA,47.61,-122.33,CITY,4.2\nPortland,OR,45.52,-122.68,CITY,4.0\nBoise,ID,43.62,-116.20,CITY,3.1\nIdaho Falls,ID,43.49,-112.03,CITY,2.6\nSalt Lake City,UT,40.76,-111.89,CITY,3.3\nProvo,UT,40.23,-111.66,CITY,2.9\nCedar City,UT,37.68,-113.06,CITY,2.8\nSt George,UT,37.10,-113.58,CITY,3.0\nRock Springs,WY,41.59,-109.20,CITY,2.3\nCheyenne,WY,41.14,-104.82,CITY,2.7\nCasper,WY,42.85,-106.32,CITY,2.5\nDenver,CO,39.74,-104.99,CITY,3.9\nColorado Springs,CO,38.83,-104.82,CITY,3.3\nPueblo,CO,38.27,-104.61,CITY,2.6\nGrand Junction,CO,39.06,-108.55,CITY,2.8\nSanta Fe,NM,35.69,-105.94,CITY,4.0\nAlbuquerque,NM,35.08,-106.65,CITY,3.3\nEl Paso,TX,31.76,-106.49,CITY,2.9\nFort Stockton,TX,30.90,-102.87,CITY,2.1\nSan Antonio,TX,29.42,-98.49,CITY,3.6\nAustin,TX,30.27,-97.74,CITY,3.9\nHouston,TX,29.76,-95.37,CITY,3.3\nDallas,TX,32.78,-96.80,CITY,3.4\nAbilene,TX,32.45,-99.73,CITY,2.3\nLubbock,TX,33.58,-101.86,CITY,2.3\nAmarillo,TX,35.19,-101.83,CITY,2.5\nOklahoma City,OK,35.47,-97.52,CITY,3.0\nTulsa,OK,36.15,-95.99,CITY,2.9\nWichita,KS,37.69,-97.34,CITY,2.7\nSalina,KS,38.84,-97.61,CITY,2.1\nKansas City,MO,39.10,-94.58,CITY,3.3\nSt Louis,MO,38.63,-90.20,CITY,3.4\nDes Moines,IA,41.60,-93.61,CITY,2.8\nOmaha,NE,41.26,-95.94,CITY,2.8\nSioux Falls,SD,43.55,-96.70,CITY,2.5\nRapid City,SD,44.08,-103.23,CITY,2.9\nLittle Rock,AR,34.75,-92.29,CITY,2.8\nJackson,MS,32.30,-90.18,CITY,2.6\nShreveport,LA,32.53,-93.75,CITY,2.5\nBaton Rouge,LA,30.45,-91.15,CITY,2.8\nNew Orleans,LA,29.95,-90.07,CITY,4.3\nMobile,AL,30.69,-88.04,CITY,2.8\nBirmingham,AL,33.52,-86.80,CITY,2.9\nPensacola,FL,30.42,-87.22,CITY,3.2\nTallahassee,FL,30.44,-84.28,CITY,2.6\nJacksonville,FL,30.33,-81.66,CITY,2.9\nOrlando,FL,28.54,-81.38,CITY,3.4\nTampa,FL,27.95,-82.46,CITY,3.3\nMiami,FL,25.76,-80.19,CITY,4.0\nPhoenix,AZ,33.45,-112.07,CITY,3.0\nTucson,AZ,32.22,-110.97,CITY,2.9\nFlagstaff,AZ,35.20,-111.65,CITY,3.2\nKingman,AZ,35.19,-114.05,CITY,2.2\nPalm Springs,CA,33.83,-116.55,CITY,3.4\nLos Angeles,CA,34.05,-118.24,CITY,4.0\nSan Diego,CA,32.72,-117.16,CITY,4.0\nLas Vegas,NV,36.17,-115.14,CITY,3.6\nReno,NV,39.53,-119.81,CITY,2.7\nElko,NV,40.83,-115.76,CITY,2.0\nBarstow,CA,34.90,-117.02,CITY,2.0\nBakersfield,CA,35.37,-119.02,CITY,2.4\nFresno,CA,36.75,-119.77,CITY,2.6\nSacramento,CA,38.58,-121.49,CITY,3.0\nSan Francisco,CA,37.77,-122.42,CITY,4.4\n"
 },
 {
  "name": "CMakeLists.txt",
  "lang": "cmake",
  "lines": 18,
  "content": "\ncmake_minimum_required(VERSION 3.10)\nproject(road_trip_optimizer CXX)\n \nset(CMAKE_CXX_STANDARD 17)\nset(CMAKE_CXX_STANDARD_REQUIRED ON)\n \nif(NOT CMAKE_BUILD_TYPE)\n  set(CMAKE_BUILD_TYPE Release)\nendif()\n \nadd_executable(road_trip src/main.cpp)\ntarget_include_directories(road_trip PRIVATE include)\n\n# Copy the location database next to the binary for convenience.\nadd_custom_command(TARGET road_trip POST_BUILD\n    COMMAND ${CMAKE_COMMAND} -E copy_directory\n        ${CMAKE_SOURCE_DIR}/data $<TARGET_FILE_DIR:road_trip>/data)"
 },
 {
  "name": "README.md",
  "lang": "markdown",
  "lines": 81,
  "content": "# Road Trip Optimizer\n\nGiven a start and a destination, finds the best-scoring chain of stops\nbetween them: national parks and natural wonders always outrank cities,\nno single day's drive exceeds 450 miles, and every stop must move you\nstrictly closer to the destination than the one before (no backtracking,\nthough the route doesn't have to be a straight line). You pick the trip\nlength in days; the optimizer fits the best scenic stops it can into that\nbudget.\n\n## Build\n\n```\ng++ -O2 -std=c++17 -Iinclude src/main.cpp -o road_trip\n```\n\nor with CMake:\n\n```\ncmake -S . -B build\ncmake --build build\n```\n\nEither way, the program expects `data/locations.csv` to be reachable at\n`data/locations.csv` relative to the working directory it's run from (the\nCMake build copies `data/` next to the binary automatically).\n\n## Run\n\n```\n./road_trip                                          # interactive prompts\n./road_trip \"Denver, CO\" \"Grand Canyon National Park\" # direct\n./road_trip \"Denver, CO\" \"Zion National Park\" 5       # 5-day trip\n./road_trip \"Denver, CO\" \"Zion National Park\" 5 300   # + 300 mi/day cap\n./road_trip --list                                    # see every known location\n```\n\nLocation names can be typed loosely (\"Zion\", \"Yellowstone\", \"Denver, CO\")\n-- matching is case-insensitive and falls back to substring search. If a\nquery matches more than one location, the program lists the candidates so\nyou can be more specific.\n\n## How it works\n\n- **Data**: `data/locations.csv` is a curated set of national parks,\n  natural wonders, and interstate-corridor cities across the continental\n  US, each with a lat/lon and a 0-5 base rating. Cities exist mainly to\n  bridge long gaps between scenic stops. Distances are estimated with the\n  haversine (great-circle) formula, since no live routing API is used --\n  real driving distance will run a bit longer, especially on winding\n  mountain roads.\n- **Scoring**: every national park / natural wonder gets a flat bonus on\n  top of its rating (`Location::score()` in `include/roadtrip/Location.hpp`)\n  large enough that any natural stop always outscores any city, regardless\n  of raw rating.\n- **Direction constraint**: a stop is only reachable if it's strictly\n  closer to the destination (great-circle distance) than the previous\n  stop. This is what \"always move toward the destination, never go the\n  opposite way\" means here -- it permits meandering detours in any\n  direction as long as net progress is made, and it works for trips in any\n  compass heading (not just east/west).\n- **Trip length**: the direction rule alone doesn't bound total mileage --\n  without a stop-count cap, the optimizer would happily zig-zag through\n  every scenic stop in the country as long as each hop is individually\n  legal. The number of days you choose caps the path length (`RouteOptimizer`\n  runs a day-bounded longest-path DP over the resulting DAG), which is what\n  keeps the result an actual road trip instead of a 20,000-mile detour\n  marathon. If you don't specify a day count, the program suggests one\n  based on the direct distance plus a little slack for detours.\n- **Optimizer**: `include/roadtrip/RouteOptimizer.hpp` builds a DAG where\n  an edge `u -> v` exists iff `v` is closer to the destination than `u` and\n  the leg is within the daily mile cap, then finds the highest-scoring path\n  from start to destination using at most the chosen number of days.\n\n## Extending\n\nAdd more rows to `data/locations.csv` (format: `name,state,lat,lon,category,rating`,\ncategory one of `NATIONAL_PARK`, `NATURAL_WONDER`, `CITY`) to widen coverage\nor fix a stretch where no route can be found because consecutive waypoints\nare more than 450 miles apart.\n"
 }
];
