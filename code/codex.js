window.SITE_CODE = window.SITE_CODE || {};
window.SITE_CODE["codex"] = [
 {
  "name": "planner.cpp",
  "lang": "cpp",
  "lines": 279,
  "content": "#include \"planner.hpp\"\n\n#include <algorithm>\n#include <cmath>\n#include <fstream>\n#include <iomanip>\n#include <iostream>\n#include <limits>\n#include <stdexcept>\n\nnamespace roadtrip {\nnamespace {\n\nconstexpr double kEarthRadiusMiles = 3958.8;\nconstexpr double kPi = 3.14159265358979323846;\n\ndouble deg_to_rad(double degrees) {\n    return degrees * kPi / 180.0;\n}\n\nbool same_coordinates(const Location& a, const Location& b) {\n    constexpr double kCoordinateTolerance = 0.0001;\n    return std::abs(a.latitude - b.latitude) <= kCoordinateTolerance &&\n           std::abs(a.longitude - b.longitude) <= kCoordinateTolerance;\n}\n\nstd::string known_name_for_coordinates(double latitude, double longitude) {\n    const Location point{\"\", latitude, longitude, 0.0, false};\n    const Location denver{\"Denver,CO\", 39.7392, -104.9903, 0.0, false};\n    const Location san_francisco{\"SanFrancisco,CA\", 37.7749, -122.4194, 0.0, true};\n\n    if (same_coordinates(point, denver)) {\n        return denver.name;\n    }\n    if (same_coordinates(point, san_francisco)) {\n        return san_francisco.name;\n    }\n    return \"\";\n}\n\n}  // namespace\n\nbool moves_toward_destination(const Location& from,\n                              const Location& to,\n                              int direction) {\n    constexpr double kTolerance = 0.0001;\n    const double delta = to.longitude - from.longitude;\n    return direction > 0 ? delta >= -kTolerance : delta <= kTolerance;\n}\n\ndouble distance_miles(const Location& a, const Location& b) {\n    const double lat1 = deg_to_rad(a.latitude);\n    const double lat2 = deg_to_rad(b.latitude);\n    const double dlat = deg_to_rad(b.latitude - a.latitude);\n    const double dlon = deg_to_rad(b.longitude - a.longitude);\n\n    const double sin_dlat = std::sin(dlat / 2.0);\n    const double sin_dlon = std::sin(dlon / 2.0);\n    const double h = sin_dlat * sin_dlat +\n                     std::cos(lat1) * std::cos(lat2) * sin_dlon * sin_dlon;\n    return 2.0 * kEarthRadiusMiles * std::asin(std::sqrt(h));\n}\n\nDataset load_dataset(const std::string& path) {\n    std::ifstream input(path);\n    if (!input) {\n        throw std::runtime_error(\"Could not open dataset: \" + path);\n    }\n\n    Dataset dataset;\n    dataset.start.name = \"Start\";\n    dataset.destination.name = \"Destination\";\n    dataset.destination.destination = true;\n\n    if (!(input >> dataset.start.latitude >> dataset.start.longitude)) {\n        throw std::runtime_error(\"Dataset is missing start coordinates.\");\n    }\n    if (!(input >> dataset.destination.latitude >> dataset.destination.longitude)) {\n        throw std::runtime_error(\"Dataset is missing destination coordinates.\");\n    }\n\n    const std::string known_start =\n        known_name_for_coordinates(dataset.start.latitude, dataset.start.longitude);\n    const std::string known_destination =\n        known_name_for_coordinates(dataset.destination.latitude, dataset.destination.longitude);\n    if (!known_start.empty()) {\n        dataset.start.name = known_start;\n    }\n    if (!known_destination.empty()) {\n        dataset.destination.name = known_destination;\n    }\n\n    int region_count = 0;\n    if (!(input >> region_count) || region_count < 0) {\n        throw std::runtime_error(\"Dataset has an invalid region count.\");\n    }\n\n    for (int region = 0; region < region_count; ++region) {\n        double region_latitude = 0.0;\n        double region_longitude = 0.0;\n        int site_count = 0;\n        if (!(input >> region_latitude >> region_longitude >> site_count) ||\n            site_count < 0) {\n            throw std::runtime_error(\"Dataset has an invalid region header.\");\n        }\n\n        for (int site = 0; site < site_count; ++site) {\n            Location location;\n            if (!(input >> location.name >> location.latitude >>\n                  location.longitude >> location.rating)) {\n                throw std::runtime_error(\"Dataset has an invalid site row.\");\n            }\n            if (same_coordinates(location, dataset.start)) {\n                dataset.start.name = location.name;\n            }\n            if (same_coordinates(location, dataset.destination)) {\n                dataset.destination.name = location.name;\n                location.destination = true;\n            }\n            dataset.candidates.push_back(location);\n        }\n    }\n\n    return dataset;\n}\n\nstd::optional<RouteResult> find_best_route_dfs(const Dataset& dataset,\n                                               int visit_stops) {\n    if (visit_stops < kMinVisitStops || visit_stops > kMaxVisitStops) {\n        throw std::invalid_argument(\"Visit stops must be between 4 and 7.\");\n    }\n\n    const int direction =\n        dataset.destination.longitude >= dataset.start.longitude ? 1 : -1;\n\n    std::vector<Location> places;\n    std::vector<int> candidate_index;\n    places.push_back(dataset.start);\n    for (int i = 0; i < static_cast<int>(dataset.candidates.size()); ++i) {\n        const auto& candidate = dataset.candidates[i];\n        if (same_coordinates(candidate, dataset.start) ||\n            same_coordinates(candidate, dataset.destination)) {\n            continue;\n        }\n        if (moves_toward_destination(dataset.start, candidate, direction) &&\n            moves_toward_destination(candidate, dataset.destination, direction)) {\n            places.push_back(candidate);\n            candidate_index.push_back(i);\n        }\n    }\n    places.push_back(dataset.destination);\n\n    const int start = 0;\n    const int destination = static_cast<int>(places.size()) - 1;\n    const int n = static_cast<int>(places.size());\n\n    std::vector<std::vector<double>> dist(n, std::vector<double>(n));\n    for (int i = 0; i < n; ++i) {\n        for (int j = 0; j < n; ++j) {\n            dist[i][j] = distance_miles(places[i], places[j]);\n        }\n    }\n\n    // Optimistic suffix bound: best possible remaining ratings, ignoring mileage.\n    std::vector<double> sorted_ratings;\n    for (int i = 1; i < destination; ++i) {\n        sorted_ratings.push_back(places[i].rating);\n    }\n    std::sort(sorted_ratings.begin(), sorted_ratings.end(), std::greater<double>());\n\n    std::vector<double> best_rating_prefix(visit_stops + 1, 0.0);\n    for (int i = 1; i <= visit_stops && i <= static_cast<int>(sorted_ratings.size()); ++i) {\n        best_rating_prefix[i] = best_rating_prefix[i - 1] + sorted_ratings[i - 1];\n    }\n    for (int i = static_cast<int>(sorted_ratings.size()) + 1; i <= visit_stops; ++i) {\n        best_rating_prefix[i] = best_rating_prefix[i - 1];\n    }\n\n    RouteResult best;\n    double best_value = -std::numeric_limits<double>::infinity();\n    std::vector<int> current;\n    std::vector<bool> used(n, false);\n    used[start] = true;\n    used[destination] = true;\n\n    auto dfs = [&](auto&& self,\n                   int at,\n                   int remaining,\n                   double rating_sum,\n                   double miles) -> void {\n        if (rating_sum + best_rating_prefix[remaining] - miles * kMileagePenalty <=\n            best_value) {\n            return;\n        }\n\n        if (remaining == 0) {\n            if (!moves_toward_destination(places[at], places[destination], direction) ||\n                dist[at][destination] > kMaxDailyMiles) {\n                return;\n            }\n\n            const double total_miles = miles + dist[at][destination];\n            const double value = rating_sum - total_miles * kMileagePenalty;\n            if (value > best_value) {\n                best_value = value;\n                best.stops = current;\n                best.rating_sum = rating_sum;\n                best.total_miles = total_miles;\n                best.route_value = value;\n            }\n            return;\n        }\n\n        for (int next = 1; next < destination; ++next) {\n            if (used[next]) {\n                continue;\n            }\n            if (!moves_toward_destination(places[at], places[next], direction)) {\n                continue;\n            }\n            if (dist[at][next] > kMaxDailyMiles) {\n                continue;\n            }\n\n            used[next] = true;\n            current.push_back(next);\n            self(self,\n                 next,\n                 remaining - 1,\n                 rating_sum + places[next].rating,\n                 miles + dist[at][next]);\n            current.pop_back();\n            used[next] = false;\n        }\n    };\n\n    dfs(dfs, start, visit_stops, 0.0, 0.0);\n\n    if (best.stops.empty()) {\n        return std::nullopt;\n    }\n\n    for (int& stop : best.stops) {\n        stop = candidate_index[stop - 1];\n    }\n    return best;\n}\n\nvoid print_route(const Dataset& dataset, const RouteResult& route) {\n    std::cout << \"Best Natural Roadtrip Route - Total Miles Driven: \"\n              << std::fixed << std::setprecision(0) << route.total_miles << \"\\n\\n\";\n\n    std::cout << \"Start: \" << dataset.start.name << \"\\n\";\n    Location previous = dataset.start;\n    for (int i = 0; i < static_cast<int>(route.stops.size()); ++i) {\n        const Location& stop = dataset.candidates[route.stops[i]];\n        std::cout << \"Stop \" << (i + 1) << \": \" << stop.name\n                  << \" | rating \" << std::fixed << std::setprecision(1)\n                  << stop.rating << \"/5 | \" << std::setprecision(0)\n                  << distance_miles(previous, stop) << \" miles from last\\n\";\n        previous = stop;\n    }\n\n    std::cout << \"Stop \" << (route.stops.size() + 1)\n              << \": \" << dataset.destination.name << \" | destination | \"\n              << std::fixed << std::setprecision(0)\n              << distance_miles(previous, dataset.destination)\n              << \" miles from last\\n\\n\";\n\n    std::cout << \"Raw place score: \" << std::fixed << std::setprecision(1)\n              << route.rating_sum << \"/\" << route.stops.size() * 5 << \"\\n\";\n    std::cout << \"Route value: \" << std::fixed << std::setprecision(3)\n              << route.route_value << \"\\n\";\n    std::cout << \"Total miles exact: \" << std::fixed << std::setprecision(1)\n              << route.total_miles << \"\\n\";\n}\n\n}  // namespace roadtrip\n"
 },
 {
  "name": "planner.hpp",
  "lang": "cpp",
  "lines": 51,
  "content": "#ifndef ROADTRIP_PLANNER_HPP\n#define ROADTRIP_PLANNER_HPP\n\n#include <optional>\n#include <string>\n#include <vector>\n\nnamespace roadtrip {\n\nconstexpr int kMinVisitStops = 4;\nconstexpr int kMaxVisitStops = 7;\nconstexpr int kDefaultVisitStops = 7;\nconstexpr double kMaxDailyMiles = 450.0;\nconstexpr double kMileagePenalty = 0.015;\n\nstruct Location {\n    std::string name;\n    double latitude = 0.0;\n    double longitude = 0.0;\n    double rating = 0.0;\n    bool destination = false;\n};\n\nstruct Dataset {\n    Location start;\n    Location destination;\n    std::vector<Location> candidates;\n};\n\nstruct RouteResult {\n    std::vector<int> stops;\n    double rating_sum = 0.0;\n    double total_miles = 0.0;\n    double route_value = 0.0;\n};\n\ndouble distance_miles(const Location& a, const Location& b);\nDataset load_dataset(const std::string& path);\nbool moves_toward_destination(const Location& from,\n                              const Location& to,\n                              int direction);\n\nstd::optional<RouteResult> find_best_route_dfs(const Dataset& dataset,\n                                               int visit_stops = kDefaultVisitStops);\n\nvoid print_route(const Dataset& dataset, const RouteResult& route);\n\n}  // namespace roadtrip\n\n#endif\n"
 },
 {
  "name": "main.cpp",
  "lang": "cpp",
  "lines": 56,
  "content": "#include \"planner.hpp\"\n\n#include <exception>\n#include <filesystem>\n#include <iostream>\n#include <string>\n#include <vector>\n\nnamespace {\n\nstd::string resolve_dataset_path(const char* executable_path, int argc, char* argv[]) {\n    if (argc >= 2) {\n        return argv[1];\n    }\n\n    const std::filesystem::path executable = std::filesystem::absolute(executable_path);\n    const std::vector<std::filesystem::path> candidates{\n        \"data/western-usa.txt\",\n        executable.parent_path() / \"data\" / \"western-usa.txt\",\n        executable.parent_path().parent_path() / \"data\" / \"western-usa.txt\",\n    };\n\n    for (const auto& candidate : candidates) {\n        if (std::filesystem::exists(candidate)) {\n            return candidate.string();\n        }\n    }\n\n    return \"data/western-usa.txt\";\n}\n\n}  // namespace\n\nint main(int argc, char* argv[]) {\n    try {\n        const std::string dataset_path = resolve_dataset_path(argv[0], argc, argv);\n        const int visit_stops =\n            argc >= 3 ? std::stoi(argv[2]) : roadtrip::kDefaultVisitStops;\n\n        const auto dataset = roadtrip::load_dataset(dataset_path);\n        const auto route = roadtrip::find_best_route_dfs(dataset, visit_stops);\n        if (!route) {\n            std::cerr << \"No feasible route found with \" << visit_stops\n                      << \" visit stops and a \" << roadtrip::kMaxDailyMiles\n                      << \"-mile daily limit.\\n\";\n            return 1;\n        }\n        roadtrip::print_route(dataset, *route);\n    } catch (const std::exception& error) {\n        std::cerr << error.what() << \"\\n\";\n        return 1;\n    }\n\n    return 0;\n}\n"
 },
 {
  "name": "test_planner.cpp",
  "lang": "cpp",
  "lines": 103,
  "content": "#include \"planner.hpp\"\n\n#include <cmath>\n#include <iostream>\n#include <set>\n#include <stdexcept>\n#include <string>\n\nnamespace {\n\nint tests_run = 0;\nint tests_failed = 0;\n\nvoid expect(bool condition, const std::string& message) {\n    ++tests_run;\n    if (!condition) {\n        ++tests_failed;\n        std::cerr << \"FAIL: \" << message << \"\\n\";\n    }\n}\n\nvoid expect_near(double actual, double expected, double tolerance, const std::string& message) {\n    expect(std::fabs(actual - expected) <= tolerance, message);\n}\n\nvoid test_dataset_loads() {\n    const auto dataset = roadtrip::load_dataset(\"data/western-usa.txt\");\n    expect(dataset.start.name == \"Denver,CO\", \"start name should come from dataset\");\n    expect(dataset.destination.name == \"SanFrancisco,CA\",\n           \"known destination coordinate should get a readable name\");\n    expect(dataset.candidates.size() == 59, \"western dataset should contain 59 candidates\");\n}\n\nvoid test_invalid_stop_counts_throw() {\n    const auto dataset = roadtrip::load_dataset(\"data/western-usa.txt\");\n    bool low_threw = false;\n    bool high_threw = false;\n\n    try {\n        (void)roadtrip::find_best_route_dfs(dataset, 3);\n    } catch (const std::invalid_argument&) {\n        low_threw = true;\n    }\n\n    try {\n        (void)roadtrip::find_best_route_dfs(dataset, 8);\n    } catch (const std::invalid_argument&) {\n        high_threw = true;\n    }\n\n    expect(low_threw, \"visit stop count below 4 should throw\");\n    expect(high_threw, \"visit stop count above 7 should throw\");\n}\n\nvoid test_western_route_constraints() {\n    const auto dataset = roadtrip::load_dataset(\"data/western-usa.txt\");\n    const auto route = roadtrip::find_best_route_dfs(dataset, 7);\n    expect(route.has_value(), \"western route should exist\");\n    if (!route) {\n        return;\n    }\n\n    expect(route->stops.size() == 7, \"7 should mean 7 visit stops before destination\");\n    expect_near(route->rating_sum, 32.8, 0.01, \"western route score should match expected\");\n    expect_near(route->total_miles, 1013.9, 0.1, \"western route miles should match expected\");\n    expect_near(route->route_value, 17.592, 0.01, \"western route value should match expected\");\n\n    const int direction =\n        dataset.destination.longitude >= dataset.start.longitude ? 1 : -1;\n    std::set<int> seen;\n    roadtrip::Location previous = dataset.start;\n    for (const int stop_index : route->stops) {\n        expect(seen.insert(stop_index).second, \"route should not repeat stops\");\n        const auto& stop = dataset.candidates[stop_index];\n        expect(roadtrip::moves_toward_destination(previous, stop, direction),\n               \"route should not reverse east/west direction\");\n        expect(roadtrip::distance_miles(previous, stop) <= roadtrip::kMaxDailyMiles + 0.01,\n               \"route leg should stay within 450 miles\");\n        previous = stop;\n    }\n    expect(roadtrip::moves_toward_destination(previous, dataset.destination, direction),\n           \"final leg should not reverse east/west direction\");\n    expect(roadtrip::distance_miles(previous, dataset.destination) <=\n               roadtrip::kMaxDailyMiles + 0.01,\n           \"final leg should stay within 450 miles\");\n}\n\n}  // namespace\n\nint main() {\n    test_dataset_loads();\n    test_invalid_stop_counts_throw();\n    test_western_route_constraints();\n\n    if (tests_failed != 0) {\n        std::cerr << tests_failed << \" of \" << tests_run << \" tests failed.\\n\";\n        return 1;\n    }\n\n    std::cout << \"All \" << tests_run << \" tests passed.\\n\";\n    return 0;\n}\n"
 },
 {
  "name": "CMakeLists.txt",
  "lang": "cmake",
  "lines": 25,
  "content": "cmake_minimum_required(VERSION 3.16)\n\nproject(roadtrip_planner LANGUAGES CXX)\n\nset(CMAKE_CXX_STANDARD 17)\nset(CMAKE_CXX_STANDARD_REQUIRED ON)\nset(CMAKE_CXX_EXTENSIONS OFF)\n\nadd_executable(roadtrip_planner\n    src/main.cpp\n    src/planner.cpp\n)\ntarget_include_directories(roadtrip_planner PRIVATE include)\n\nadd_executable(planner_tests\n    tests/test_planner.cpp\n    src/planner.cpp\n)\ntarget_include_directories(planner_tests PRIVATE include)\n\nfile(COPY data DESTINATION ${CMAKE_BINARY_DIR})\n\nenable_testing()\nadd_test(NAME planner_tests COMMAND planner_tests)\n"
 },
 {
  "name": "original-codex.txt",
  "lang": "text",
  "lines": 67,
  "content": "39.7392 -104.9903\n37.7749 -122.4194\n31\n39.7392 -104.9903 1\nDenver,CO 39.7392 -104.9903 0.5\n37.7749 -122.4194 1\nSanFrancisco,CA 37.7749 -122.4194 0.5\n36.1716 -115.1391 1\nLasVegas,NV 36.1716 -115.1391 0.4\n34.0522 -118.2437 1\nLosAngeles,CA 34.0522 -118.2437 0.4\n40.7608 -111.8910 1\nSaltLakeCity,UT 40.7608 -111.8910 0.4\n39.0997 -94.5786 1\nKansasCity,MO 39.0997 -94.5786 0.4\n41.8781 -87.6298 1\nChicago,IL 41.8781 -87.6298 0.5\n43.8554 -102.3397 1\nBadlandsNP 43.8554 -102.3397 4.7\n40.3428 -105.6836 1\nRockyMountainNP 40.3428 -105.6836 4.8\n37.7916 -105.5943 1\nGreatSandDunesNP 37.7916 -105.5943 4.7\n38.5754 -107.7416 1\nBlackCanyonNP 38.5754 -107.7416 4.5\n38.7331 -109.5925 1\nArchesNP 38.7331 -109.5925 4.9\n38.3269 -109.8783 1\nCanyonlandsNP 38.3269 -109.8783 4.8\n38.3669 -111.2621 1\nCapitolReefNP 38.3669 -111.2621 4.7\n37.5930 -112.1871 1\nBryceCanyonNP 37.5930 -112.1871 4.8\n37.2982 -113.0263 1\nZionNP 37.2982 -113.0263 5.0\n36.2679 -112.3535 1\nGrandCanyonNP 36.2679 -112.3535 5.0\n36.4289 -114.5148 1\nValleyOfFireSP 36.4289 -114.5148 4.4\n36.5054 -117.0794 1\nDeathValleyNP 36.5054 -117.0794 4.9\n33.8734 -115.9010 1\nJoshuaTreeNP 33.8734 -115.9010 4.6\n36.4864 -118.5658 1\nSequoiaNP 36.4864 -118.5658 4.8\n36.8879 -118.5551 1\nKingsCanyonNP 36.8879 -118.5551 4.7\n37.8651 -119.5383 1\nYosemiteNP 37.8651 -119.5383 5.0\n39.0968 -120.0324 1\nLakeTahoe 39.0968 -120.0324 4.6\n38.0834 -122.8636 1\nPointReyesNS 38.0834 -122.8636 4.5\n41.2132 -124.0046 1\nRedwoodNP 41.2132 -124.0046 4.9\n35.6118 -83.4895 1\nGreatSmokyMountainsNP 35.6118 -83.4895 4.9\n37.1862 -86.1005 1\nMammothCaveNP 37.1862 -86.1005 4.5\n37.9263 -81.1547 1\nNewRiverGorgeNP 37.9263 -81.1547 4.6\n38.4755 -78.4535 1\nShenandoahNP 38.4755 -78.4535 4.7\n44.3386 -68.2733 1\nAcadiaNP 44.3386 -68.2733 4.8\n\n"
 }
];
